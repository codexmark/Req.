{
  "name": "req-codex",
  "version": "1.0.0",
  "description": "Req. Codex with elicitation and cards modules",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp -r app/* public/"
  },
  "keywords": [
    "cards",
    "discord",
    "webhook"
  ],
  "author": "codexmark",
  "license": "MIT",
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}
